/*
 * Generated by Abacus.
 */
package com.landawn.abacus.entity.stringId;

import java.util.List;
import java.sql.Timestamp;
import java.util.Objects;
import com.landawn.abacus.core.AbstractDirtyMarker;
import com.landawn.abacus.entity.stringId.StringIdPNL;
import javax.xml.bind.annotation.XmlTransient;
import com.landawn.abacus.annotation.Type;


/**
 * Generated by Abacus.
 * @version ${version}
 */
public class Author extends AbstractDirtyMarker implements StringIdPNL.AuthorPNL {
    private long id;
    private String firstName;
    private String lastName;
    private Timestamp birthDay;
    private Contact contact;
    private List<Book> book;

    public Author() {
        super(__);
    }

    public Author(long id) {
        this();

        setId(id);
    }

    public Author(String firstName, String lastName, Timestamp birthDay, Contact contact, 
        List<Book> book) {
        this();

        setFirstName(firstName);
        setLastName(lastName);
        setBirthDay(birthDay);
        setContact(contact);
        setBook(book);
    }

    public Author(long id, String firstName, String lastName, Timestamp birthDay, 
        Contact contact, List<Book> book) {
        this();

        setId(id);
        setFirstName(firstName);
        setLastName(lastName);
        setBirthDay(birthDay);
        setContact(contact);
        setBook(book);
    }

    @XmlTransient
    public boolean isDirty() {
        return super.isDirty()
               || (contact == null ? false : contact.isDirty())
               || (book == null ? false : super.isEntityDirty(book));
    }

    public void markDirty(boolean isDirty) {
        super.markDirty(isDirty);

        if (contact != null) {
            contact.markDirty(isDirty);
        }

        if (book != null) {
            super.markEntityDirty(book, isDirty);
        }
    }

    @Type("long")
    public long getId() {
        return id;
    }

    public Author setId(long id) {
        super.setUpdatedPropName(ID);
        this.id = id;

        return this;
    }

    @Type("String")
    public String getFirstName() {
        return firstName;
    }

    public Author setFirstName(String firstName) {
        super.setUpdatedPropName(FIRST_NAME);
        this.firstName = firstName;

        return this;
    }

    @Type("String")
    public String getLastName() {
        return lastName;
    }

    public Author setLastName(String lastName) {
        super.setUpdatedPropName(LAST_NAME);
        this.lastName = lastName;

        return this;
    }

    @Type("Timestamp")
    public Timestamp getBirthDay() {
        return birthDay;
    }

    public Author setBirthDay(Timestamp birthDay) {
        super.setUpdatedPropName(BIRTH_DAY);
        this.birthDay = birthDay;

        return this;
    }

    @Type("com.landawn.abacus.entity.stringId.Contact")
    public Contact getContact() {
        return contact;
    }

    public Author setContact(Contact contact) {
        super.setUpdatedPropName(CONTACT);
        this.contact = contact;

        return this;
    }

    @Type("List<com.landawn.abacus.entity.stringId.Book>")
    public List<Book> getBook() {
        return book;
    }

    public Author setBook(List<Book> book) {
        super.setUpdatedPropName(BOOK);
        this.book = book;

        return this;
    }

    public int hashCode() {
        int h = 17;
        h = 31 * h + Objects.hashCode(id);
        h = 31 * h + Objects.hashCode(firstName);
        h = 31 * h + Objects.hashCode(lastName);
        h = 31 * h + Objects.hashCode(birthDay);
        h = 31 * h + Objects.hashCode(contact);
        h = 31 * h + Objects.hashCode(book);

        return h;
    }

    public boolean equals(Object obj) {
        if (this == obj) {
            return true;
        }

        if (obj instanceof Author) {
            final Author other = (Author) obj;

            return Objects.equals(id, other.id)
                && Objects.equals(firstName, other.firstName)
                && Objects.equals(lastName, other.lastName)
                && Objects.equals(birthDay, other.birthDay)
                && Objects.equals(contact, other.contact)
                && Objects.equals(book, other.book);
        }

        return false;
    }

    public String toString() {
         return "{id=" + Objects.toString(id)
                 + ", firstName=" + Objects.toString(firstName)
                 + ", lastName=" + Objects.toString(lastName)
                 + ", birthDay=" + Objects.toString(birthDay)
                 + ", contact=" + Objects.toString(contact)
                 + ", book=" + Objects.toString(book)
                 + "}";
    }
}
